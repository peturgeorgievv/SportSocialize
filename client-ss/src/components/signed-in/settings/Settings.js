import React from 'react';
import Header from '../Header';

const Settings = () => {
  return (
    <div>
      <Header />
      Some settings
    </div>
  );
};

export default Settings;
